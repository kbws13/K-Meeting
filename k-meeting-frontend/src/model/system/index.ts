export interface SysSetting {
  openCamera: boolean
  openMic: boolean
  screencapFolder: string
}

export interface AdminSystemSetting {
  maxImageSize: number
  maxVideoSize: number
  maxFileSize: number
}
